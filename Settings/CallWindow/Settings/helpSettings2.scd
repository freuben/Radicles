[ [ 'add', [ 'str', 'num', 'num' ], {|str, num1, num2| (num1 + num2).postln;}, "adds two numbers" ], [ 'ls', [ 'str' ], {c.listSettings;}, "prints one item commnads" ], [ 'mul', [ 'str', 'num', 'num' ], {|str, num1, num2| (num1 * num2).postln;}, "multiply two numbers" ], [ 'la', [ 'str', 'num' ], {|a,b| (a ++ " is this shit and " ++ b.asString ++ " is the shat").postln; }, "array of cmds" ], [ 'le', [ 'str', 'num' ], {|a,b| (a ++ " is this shit and " ++ b.asString ++ " is the shat").postln; }, "array of cmds" ], [ 'li', [ 'str', 'num' ], {|a,b| (a ++ " is this shit and " ++ b.asString ++ " is the shat").postln; }, "array of cmds" ], [ 'lo', [ 'str', 'num' ], {|a,b| (a ++ " is this shit and " ++ b.asString ++ " is the shat").postln; }, "array of cmds" ], [ 'lu', [ 'str', 'num' ], {|a,b| (a ++ " is this shit and " ++ b.asString ++ " is the shat").postln; }, "array of cmds" ] ]